{
  "name": "api-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test jest --coverage --verbose",
    "dev": "NODE_ENV=development nodemon index.js",
    "//comment": "in these scripts we are sitting some enviroment variables (even if they are not declared in the .env) before starting to run the script so if you run the dev it won't have the same port set when you run the start",
    "//comment2": "NODE_ENV is a special enviroment variable , read more about it, ",
    "start": "PORT=3000 NODE_ENV=production node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "jest": "^29.5.0",
    "pg": "^8.11.0",
    "sequelize": "^6.32.0",
    "sequelize-cli": "^6.6.1",
    "sqlite": "^4.2.1",
    "sqlite3": "^5.1.6",
    "supertest": "^6.3.3"
  }
}
